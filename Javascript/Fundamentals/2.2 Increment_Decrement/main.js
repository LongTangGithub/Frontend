



// ! Postfix
// todo: Increment After Evaluation
// ? The value of the variable is returned first and then the variable is incremented
let number = 0;
console.log(number++);           // 0
console.log(number);            // 1   

// ! Prefix
// todo: Increment Before Evaluation
// ? The variable is incremented first, and then the new value of the variable is returned
let num2 = 0;
console.log(++num2);         // 1
